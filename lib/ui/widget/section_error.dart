import 'package:flutter/material.dart';

class SectionError extends StatelessWidget {
  final VoidCallback onPressed;

  SectionError({this.onPressed});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
